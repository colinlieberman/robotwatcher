<%= form_for(pool_reading) do |f| %>
  <% if pool_reading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pool_reading.errors.count, "error") %> prohibited this pool_reading from being saved:</h2>

      <ul>
      <% pool_reading.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
